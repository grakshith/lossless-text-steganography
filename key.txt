KEY:

If text stego:

Huffman table || Length of message || Random seed || Alpha || Beta

If audio stego:

Huffman table || Length of message || Random seed 


0000 0000 1111 1101
0000 0001 1111 1000 